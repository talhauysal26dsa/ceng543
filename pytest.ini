[tool:pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# Exclude manual tests (not pytest compatible)
norecursedirs = tests/manual .git __pycache__ venv

addopts = 
    -v
    --tb=short
    --strict-markers
    -p no:warnings
